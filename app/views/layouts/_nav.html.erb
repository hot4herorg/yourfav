<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to 'YourFavorite XXX', root_path, class: 'navbar-brand' %>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

			<ul class="nav navbar-nav">

				<li class="dropdown">
					<a class="dropdown-toggle" href="#" data-toggle="dropdown">Add Video <strong class="caret"></strong></a>
					<div class="dropdown-menu" style="padding: 15px; min-width: 300px;">
						<form>
							<div class="form-group">
								<label for="video_url">Video URL</label>
								<input type="email" class="form-control" id="video_url" placeholder="URL">
							</div>
							<button type="submit" class="btn btn-sm btn-block btn-info">Add Video</button>
						</form>
					</div>
				</li>

				<%= content_tag :li, link_to('Add Video', new_video_path) %>
				<%= content_tag :li, link_to('Galleries', galleries_path) %>
				<%= content_tag :li, link_to('Gallery', Gallery.all.sample(1), data: {toggle: :modal, target: '#gallery-modal'}) %>
			</ul>

			<ul class="nav navbar-nav navbar-right">
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
						Admin
						<span class="caret"></span>
					</a>
					<ul class="dropdown-menu">
						<%= content_tag :li, link_to('Sites', sites_path) %>
						<li role="separator" class="divider"></li>
						<li><a href="#">Separated link</a></li>
					</ul>
				</li>
			</ul>

		</div><!-- /.navbar-collapse -->
	</div><!-- /.container -->
</nav>
