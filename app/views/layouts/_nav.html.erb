<nav class="navbar navbar-inverse navbar-fixed-top">
	<div class="container">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to 'YourFavorite', root_path, class: 'navbar-brand' %>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

			<ul class="nav navbar-nav">
				<%= content_tag :li, link_to('Favorites', user_path(current_user)) if user_signed_in? %>
				<%= content_tag :li, link_to('Galleries', user_galleries_path(current_user)) if user_signed_in? %>
			</ul>

			<ul class="nav navbar-nav navbar-right">

				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
						<i class="fa fa- fa-user"></i> <span class="caret"></span>
					</a>
					<ul class="dropdown-menu">

						<%= content_tag :li, link_to('Sites', sites_path) %>

						<li role="separator" class="divider"></li>
						<li>
							<%= link_to_unless user_signed_in?, 'Sign in', new_user_session_path do %>
							<%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
							<% end %>
						</li>

					</ul>
				</li>
			</ul>

			<ul class="nav navbar-nav navbar-right">

				<li class="dropdown">
					<a class="dropdown-toggle" href="#" data-toggle="dropdown">Add Video <strong class="caret"></strong></a>
					<div id="nav-add-video-dropdown" class="dropdown-menu">

						<p>
							<%= form_for Video.new(add_by_url: true) do |f| %>
								<div class="input-group">
									<%= f.hidden_field :add_by_url %>
									<%= f.text_field :url, class: 'form-control', placeholder: 'Video URL' %>
									<span class="input-group-btn">
										<button class="btn btn-warning" type="submit">
											<i class="fa fa-plus"></i>
										</button>
									</span>
								</div>
							<% end %>
						</p>

						<p class="text-center">
							<%= link_to 'Browser Extension', new_video_path %>
						</p>

					</div>
				</li>


			</ul>

		</div><!-- /.navbar-collapse -->
	</div><!-- /.container -->
</nav>
